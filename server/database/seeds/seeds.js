const adopterProfiles = require('./adopterSeed');
const animalProfiles = require('./animalsSeed');
const pastNotifications = require('./pastNotificationsSeed');

exports.seed = function(knex) {
    //delete all existing entries for table
}